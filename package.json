{
  "name": "headmaster-command-center",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace client",
    "check": "npm run lint && npm test && npm run build",
    "dev": "concurrently -k -n CLIENT,SERVER -c blue,yellow \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "lint": "eslint client/src server/src server/test",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace client && npm run test --workspace server"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "concurrently": "10.0.4",
    "eslint": "10.0.0",
    "globals": "17.11.0",
    "jsdom": "29.1.1",
    "typescript-eslint": "8.67.0"
  },
  "engines": {
    "node": ">=22"
  }
}
